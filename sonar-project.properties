# must be unique in a given SonarQube instance
sonar.projectKey=client-summary-application
# this is the name and version displayed in the SonarQube UI. Was mandatory prior to SonarQube 6.1.
sonar.projectName=client-summary-application
sonar.projectVersion=1.0
sonar.javascript.lcov.reportPaths=jest-reports/lcov.info

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# This property is optional if sonar.modules is set.
sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

sonar.exclusions=**/node_modules/**,Jenkinsfile,**/jest-reports/**
sonar.coverage.exclusions=**/node_modules/**,Jenkinsfile,**/jest-reports/**,**/*.test.tsx


